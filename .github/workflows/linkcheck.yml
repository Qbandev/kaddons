name: Weekly Link Check

on:
  schedule:
    - cron: "0 8 * * 1" # Every Monday at 08:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: linkcheck-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  linkcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: actions/setup-go@7a3fe6cf4cb3a834922a1244abfce67bcef6a0c5 # v6.2.0
        with:
          go-version: '1.25.7'
          cache: true

      - name: Run link checker
        id: check
        run: |
          set -euo pipefail
          set +e
          go run ./cmd/kaddons-validate --links > report.md
          status=$?
          set -e
          if [ "$status" -eq 0 ]; then
            echo "broken=false" >> "$GITHUB_OUTPUT"
          elif [ "$status" -eq 1 ]; then
            echo "broken=true" >> "$GITHUB_OUTPUT"
          else
            echo "::error::Link checker runtime failure (exit $status)"
            cat report.md
            exit "$status"
          fi
          cat report.md

      - name: Create or update issue
        if: steps.check.outputs.broken == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          TITLE="Broken links in k8s_universal_addons.json"
          LABEL="broken-links"

          # Ensure label exists
          gh label create "$LABEL" --description "Automated broken link reports" --color "d93f0b" 2>/dev/null || true

          BODY="$(cat report.md)"$'\n\n'"_Last checked: $(date -u '+%Y-%m-%d %H:%M UTC')_"

          EXISTING=$(gh issue list --label "$LABEL" --state open --json number --jq '.[0].number // empty')

          if [ -n "$EXISTING" ]; then
            gh issue edit "$EXISTING" --body "$BODY"
            echo "Updated issue #$EXISTING"
          else
            gh issue create --title "$TITLE" --label "$LABEL" --body "$BODY"
          fi
